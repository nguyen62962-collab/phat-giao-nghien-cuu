# Dữ liệu thô + ảnh tải về: tái tạo được mỗi lần chạy, không cần lưu lịch sử
nghien-cuu/du-lieu/
nghien-cuu/thumbnail-doi-thu/

# Báo cáo (.md + .pdf) THÌ GIỮ LẠI — đây là sản phẩm chính, cần đọc lại được
# từ GitHub (điện thoại) và tồn tại qua các lần sandbox cloud bị huỷ.
# (không ignore nghien-cuu/bao-cao/)

__pycache__/
*.pyc

# Thông tin đăng nhập YouTube Analytics API (OAuth) — TUYỆT ĐỐI không đẩy lên
# GitHub, đây là quyền truy cập riêng tư vào dữ liệu kênh của bạn.
secrets/
client_secret*.json
youtube_analytics_token.json

# Dữ liệu Analytics kênh mình (riêng tư, tái tạo được bằng cách chạy lại script)
nghien-cuu/du-lieu-kenh-minh/
